import { json,bodyOf,preflight,methodNotAllowed } from "./_shared/http.js";
import { currentUser,fail,requireRoles } from "./_shared/supabase.js";
const STAFF_FIELDS=["name","email","phone","role","active","hourly_rate","hire_date","federal_tax_rate","state_tax_rate","local_tax_rate","other_deduction_rate","fixed_deduction"];
export async function handler(event){const ready=preflight(event);if(ready)return ready;try{const ctx=await currentUser(event);requireRoles(ctx,['owner','manager']);const{client,shopId}=ctx;if(event.httpMethod==="GET"){const{data:staff,error}=await client.from("staff").select("*").eq("shop_id",shopId).order("created_at",{ascending:false});if(error)throw error;const{data:entries,error:te}=await client.from("staff_time_entries").select("*").eq("shop_id",shopId).order("clock_in",{ascending:false});if(te)throw te;return json(200,{items:staff||[],time_entries:entries||[]})}if(event.httpMethod==="POST"){const body=bodyOf(event);if(body.action==="CLOCK_IN"){const{data:open}=await client.from("staff_time_entries").select("id").eq("shop_id",shopId).eq("staff_id",body.staff_id).is("clock_out",null).maybeSingle();if(open)throw new Error("This employee is already clocked in.");const{data,error}=await client.from("staff_time_entries").insert({shop_id:shopId,staff_id:body.staff_id,clock_in:new Date().toISOString()}).select("*").single();if(error)throw error;return json(201,{item:data})}if(body.action==="CLOCK_OUT"){const{data:open,error:oe}=await client.from("staff_time_entries").select("*").eq("shop_id",shopId).eq("staff_id",body.staff_id).is("clock_out",null).order("clock_in",{ascending:false}).limit(1).maybeSingle();if(oe)throw oe;if(!open)throw new Error("This employee is not clocked in.");const out=new Date(),hours=Math.max(0,(out-new Date(open.clock_in))/3600000);const{data,error}=await client.from("staff_time_entries").update({clock_out:out.toISOString(),hours_worked:Number(hours.toFixed(2))}).eq("id",open.id).eq("shop_id",shopId).select("*").single();if(error)throw error;return json(200,{item:data})}const payload={shop_id:shopId,active:true};for(const f of STAFF_FIELDS)if(f in body&&body[f]!=="")payload[f]=body[f];const{data,error}=await client.from("staff").insert(payload).select("*").single();if(error)throw error;return json(201,{item:data})}if(event.httpMethod==="PATCH"){const body=bodyOf(event);if(!body.id)throw new Error("Missing employee id");const payload={};for(const f of STAFF_FIELDS)if(f in body&&body[f]!=="")payload[f]=body[f];const{data,error}=await client.from("staff").update(payload).eq("id",body.id).eq("shop_id",shopId).select("*").single();if(error)throw error;return json(200,{item:data})}if(event.httpMethod==="DELETE"){const body=bodyOf(event);if(!body.id)throw new Error("Missing employee id");const{error}=await client.from("staff").delete().eq("id",body.id).eq("shop_id",shopId);if(error)throw error;return json(200,{ok:true})}return methodNotAllowed()}catch(error){return fail(error)}}
