# Bloom v8.5.1

- Tax is entered as a percentage instead of a dollar amount.
- New orders default to a 6% tax rate.
- Sales tax is calculated automatically from the subtotal.
- Order total updates live as subtotal, tax rate, or delivery fee changes.
- Tax and totals are rounded to two decimal places.
- Preserves the required authenticated `user_id` when creating orders.
