# Bloom v22.2 — Employee File PIN Fix

- Employee files now require the employee PIN before private details open.
- Incorrect PINs are rejected by the server.
- Staff list responses now contain only public summary fields.
- Existing employees without a PIN may be opened once by an authenticated owner or manager so a PIN can be created.
- PIN copy now clearly explains that it protects both the private employee file and clock controls.
